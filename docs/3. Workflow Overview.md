# Workflow Overview

## Core Workflows

### Workflow 1: Data Upload and Validation

Users upload CSV data files containing leg, test, FTE, and equipment information.

```mermaid
sequenceDiagram
    participant User
    participant Frontend
    participant Backend
    participant FileSystem

    User->>Frontend: Select CSV files
    Frontend->>Frontend: Validate format locally
    Frontend->>Backend: POST /api/v1/file-upload
    Backend->>FileSystem: Store files
    FileSystem-->>Backend: Confirm storage
    Backend->>Backend: Validate data integrity
    Backend-->>Frontend: Validation results
    Frontend->>User: Show validation status
    Frontend->>Frontend: Update fileStore
```

**Steps:**
1. User clicks file upload component and selects CSV files
2. Frontend performs preliminary format validation
3. Files are uploaded to backend via multipart/form-data
4. Backend stores files and validates content (column headers, data types)
5. Validation results returned to UI with any errors/warnings
6. FileStore updated with file references and validation state

### Workflow 2: Configuration Setup

Users configure priority modes, constraints, and solver parameters.

```mermaid
sequenceDiagram
    participant User
    participant Frontend
    participant ConfigStore
    participant Backend

    User->>Frontend: Select priority mode
    Frontend->>ConfigStore: Update priorityMode
    User->>Frontend: Set deadline constraints
    Frontend->>ConfigStore: Update deadlines
    User->>Frontend: Define proximity rules
    Frontend->>ConfigStore: Update proximityRules
    Note over Frontend,ConfigStore: Changes persist to localStorage
    Frontend->>Backend: POST /api/v1/validation
    Backend->>Backend: Validate configuration
    Backend-->>Frontend: Validation response
```

**Steps:**
1. User navigates to Configuration tab
2. Select priority mode (leg_priority, end_date_priority, etc.)
3. Add/edit deadline constraints for tests
4. Define proximity rules (tests that must run close together)
5. Set penalty settings for constraint violations
6. Frontend validates with backend API
7. Configuration persists to browser localStorage

### Workflow 3: Solver Execution (Single Scenario)

Execute the optimization solver for a single configuration.

```mermaid
sequenceDiagram
    participant User
    participant Frontend
    participant Backend
    participant SolverProcess
    participant FileSystem

    User->>Frontend: Click "Solve"
    Frontend->>Frontend: Capture config snapshot
    Frontend->>Backend: POST /api/solver/execute
    Backend->>FileSystem: Create run directory
    Backend->>Backend: Generate execution ID
    Backend->>SolverProcess: Fork subprocess
    SolverProcess->>FileSystem: Read input data
    SolverProcess->>SolverProcess: Build CP-SAT model
    SolverProcess->>SolverProcess: Solve optimization
    loop Progress Updates
        SolverProcess-->>Backend: Progress via stdout
        Backend-->>Frontend: SSE event (progress)
        Frontend->>User: Update progress bar
    end
    SolverProcess->>FileSystem: Write schedule.csv
    SolverProcess->>FileSystem: Write reports
    SolverProcess-->>Backend: Exit with results
    Backend-->>Frontend: SSE event (completed)
    Frontend->>User: Show success notification
```

**Steps:**
1. User clicks "Solve" button in Solver Controls
2. Frontend captures current configuration and data snapshots
3. POST request to `/api/solver/execute` with configuration payload
4. Backend creates run directory with timestamp
5. Backend forks solver subprocess with configuration
6. Solver loads data, builds model, runs CP-SAT optimization
7. Progress events stream to frontend via SSE
8. On completion, solver writes schedule and reports to file system
9. Backend captures solver output and closes stream
10. Frontend shows completion and offers to view results

### Workflow 4: Batch Execution

Run multiple scenarios with different configurations in one batch.

```mermaid
sequenceDiagram
    participant User
    participant Frontend
    participant Backend
    participant BatchMgr
    participant SolverProcess

    User->>Frontend: Define scenario variants
    User->>Frontend: Click "Run Batch"
    Frontend->>Backend: POST /api/runs/batch
    Backend->>BatchMgr: Create batch job
    BatchMgr-->>Backend: Batch ID
    Backend-->>Frontend: Batch created + SSE stream URL
    loop For each scenario
        Frontend->>Frontend: Display "Running Scenario X"
        Backend->>SolverProcess: Execute scenario
        SolverProcess-->>Backend: Results
        Backend-->>Frontend: SSE event (scenario complete)
    end
    Backend-->>Frontend: SSE event (batch complete)
    Frontend->>User: Show batch summary
```

**Steps:**
1. User defines multiple scenario configurations in Batch Editor
2. Each scenario has unique config overrides
3. User initiates batch execution
4. Backend creates batch job with queue of scenarios
5. Frontend opens SSE stream for progress updates
6. Each scenario executes sequentially or in parallel
7. Progress events update individual scenario status
8. After all scenarios complete, batch summary generated
9. Results available for comparison

### Workflow 5: Results Visualization

View and analyze solver output through charts and tables.

```mermaid
flowchart LR
    A[Run Completed] --> B{Load Results}
    B --> C[schedule.csv]
    B --> D[resource_utilization.csv]
    B --> E[plots/*.png]
    
    C --> F[Gantt Chart]
    C --> G[Test Timeline]
    
    D --> H[FTE Usage Graph]
    D --> I[Equipment Graph]
    D --> J[Concurrency Chart]
    
    E --> K[Static Plots]
    
    F --> L[Interactive Visualization Tab]
    G --> L
    H --> L
    I --> L
    J --> L
    K --> L
```

**Steps:**
1. User navigates to Visualizer tab
2. Frontend loads latest run results from file system
3. D3.js renders interactive Gantt chart
4. Resource utilization graphs show FTE/equipment usage over time
5. Concurrency chart shows parallel test execution
6. Users can filter, zoom, and export visualizations

## Data Flow

### Input Data Flow

```mermaid
flowchart LR
    subgraph UserInput[User Input Files]
        L[data_legs.csv]
        T[data_test.csv]
        F[data_fte.csv]
        E[data_equipment.csv]
        D[data_test_duts.csv]
    end
    
    subgraph Validation[Data Validation]
        V[Validate Headers]
        V2[Validate Types]
        V3[Cross-Reference]
    end
    
    subgraph Transformation[Data Transformation]
        DL[Data Loader]
        P[PlanningData Object]
    end
    
    L --> V
    T --> V
    F --> V
    E --> V
    D --> V
    
    V --> V2 --> V3 --> DL --> P
```

### Solver Data Flow

```mermaid
flowchart TB
    Input[PlanningData] --> Model[CP-SAT Model]
    
    subgraph ModelConstruction[Model Building]
        Vars[Decision Variables<br/>test start times]
        Constraints[Constraints<br/>temporal, resource, precedence]
        Objective[Objective Function<br/>priority-based]
    end
    
    Model --> Vars --> Constraints --> Objective
    Objective --> Solve[CP-SAT Solver]
    
    Solve --> |Optimal/Feasible| Solution[Solution Values]
    Solution --> Reports[Report Generation]
    
    subgraph Outputs[Output Files]
        Schedule[schedule.csv]
        Resources[resource_utilization.csv]
        Plots[plots/*.png]
    end
    
    Reports --> Schedule
    Reports --> Resources
    Reports --> Plots
```

### API Data Flow

```mermaid
flowchart LR
    subgraph Client
        UI[Browser UI]
    end
    
    subgraph API[Backend API]
        Endpoints[REST Endpoints]
        Services[Service Layer]
        State[In-Memory State]
    end
    
    subgraph Persistence[File System]
        Runs[runs/]
        Input[input_data/]
    end
    
    UI -->|HTTP/JSON| Endpoints
    Endpoints --> Services
    Services -->|Read/Write| Runs
    Services -->|Read| Input
    Services -->|Cache| State
    
    UI -->|SSE| Services
    Services -->|Progress Events| UI
```

## State Management

### Frontend State (Alpine.js Stores)

| Store | Purpose | Key Properties |
|-------|---------|----------------|
| `fileStore` | Uploaded file references | `files`, `validationErrors` |
| `configStore` | Solver configuration | `priorityMode`, `deadlines`, `proximityRules`, `penaltySettings` |
| `solverStore` | Solver execution state | `status`, `executionId`, `progress`, `results` |
| `batchStore` | Batch job management | `scenarios`, `batchStatus`, `results[]` |
| `visualizationStore` | Chart state | `scheduleData`, `resourceData`, `selectedView` |
| `uiStore` | UI preferences | `activeTab`, `notifications`, `theme` |

**Persistence:**
- Critical state persisted to `localStorage`
- Session data (active runs) cleared on page close
- Configuration auto-saved on changes

### Backend State (In-Memory)

| Data Structure | Purpose | Lifecycle |
|---------------|---------|-----------|
| `ExecutionState` | Active solver executions | Created on solve, deleted on completion |
| `SessionState` | User session data | Created on first API call, TTL-based expiry |
| `BatchState` | Batch job tracking | Created on batch start, persisted to files |

**State Access Pattern:**
```python
# Service accesses state via store
state = store.get_execution(execution_id)
if state is None:
    raise NotFoundError(f"Execution {execution_id} not found")
```

### Solver State (File-Based)

| Directory | Contents | Purpose |
|-----------|----------|---------|
| `runs/{run_id}/` | Run-specific files | Isolated execution outputs |
| `runs/{run_id}/input/` | Input data snapshot | Reproducibility |
| `runs/{run_id}/output/` | Solver outputs | Results and reports |

## Error Handling

### Frontend Error Handling

```mermaid
flowchart TD
    A[User Action] --> B{API Call}
    B -->|Success| C[Update Store]
    B -->|Network Error| D[Show Notification<br/>Network unavailable]
    B -->|Validation Error| E[Show Field Errors]
    B -->|Server Error| F[Show Notification<br/>Try again later]
    
    C --> G[Update UI]
    D --> H[Retry Option]
    E --> I[Correct Input]
    F --> J[Contact Support]
```

**Error Display:**
- Toast notifications for transient errors
- Inline validation for form errors
- Error boundary for component failures

### Backend Error Handling

```mermaid
flowchart TD
    A[Request] --> B[Route Handler]
    B --> C{Validation}
    C -->|Invalid| D[422 Unprocessable Entity]
    C -->|Valid| E[Service Layer]
    E --> F{Business Logic}
    F -->|Error| G[HTTPException]
    F -->|Success| H[Response Model]
    G --> I[Error Response]
    
    J[Global Exception Handler] --> K[Log Error]
    K --> L[500 Internal Server Error]
```

**Exception Hierarchy:**
- `HTTPException`: Client errors (4xx)
- `ServiceException`: Business logic errors
- `InternalServerError`: Unexpected failures (5xx)

### Solver Error Handling

```mermaid
flowchart TD
    A[Solver Start] --> B{Load Data}
    B -->|File Error| C[Exit Code 1<br/>Data Error]
    B -->|Success| D{Build Model}
    D -->|Infeasible| E[Exit Code 2<br/>Model Error]
    D -->|Success| F{Solve}
    F -->|Infeasible| G[Exit Code 3<br/>No Solution]
    F -->|Timeout| H[Exit Code 4<br/>Timeout]
    F -->|Success| I[Exit Code 0<br/>Success]
    
    J[Any Status] --> K[Stdout/Stderr Captured]
    K --> L[Backend Parses Output]
```

**Error Propagation:**
- Solver exit codes mapped to error states
- Stderr captured and shown to user
- Partial results saved on failure if possible