# planning_test_program Development Guidelines

Auto-generated from all feature plans. Last updated: 2025-11-19

## Active Technologies
- JavaScript (ES6+), Alpine.js 3.x + Alpine.js (Core), CodeMirror 6 (Editor), D3.js v7 (Visualization) (002-d3-vis-port)
- Browser LocalStorage (for persisting user code edits) (002-d3-vis-port)
- JavaScript (ES6+), HTML5, CSS3 + Alpine.js 3.x, D3.js v7, PapaParse 5.3.2 (for CSV parsing) (003-visualizer-integration)
- Browser localStorage (for visualization state persistence), in-memory state (Alpine.js reactive data) (003-visualizer-integration)

- Python 3.8+ (for backend API), JavaScript/ES6+ (for Alpine.js UI) + FastAPI (backend API), Alpine.js 3.x (UI framework), planner_v4 (Python solver module), pandas (data processing) (001-solver-ui-integration)

## Project Structure

```text
backend/
frontend/
tests/
```

## Commands

cd src; pytest; ruff check .

## Code Style

Python 3.8+ (for backend API), JavaScript/ES6+ (for Alpine.js UI): Follow standard conventions

## Recent Changes
- 003-visualizer-integration: Added JavaScript (ES6+), HTML5, CSS3 + Alpine.js 3.x, D3.js v7, PapaParse 5.3.2 (for CSV parsing)
- 002-d3-vis-port: Added JavaScript (ES6+), Alpine.js 3.x + Alpine.js (Core), CodeMirror 6 (Editor), D3.js v7 (Visualization)

- 001-solver-ui-integration: Added Python 3.8+ (for backend API), JavaScript/ES6+ (for Alpine.js UI) + FastAPI (backend API), Alpine.js 3.x (UI framework), planner_v4 (Python solver module), pandas (data processing)

## Alpine.js Component Examples

### Basic Component Structure
```html
<div x-data="{ count: 0, items: [] }"
     x-init="fetchData()">
  <button x-on:click="count++">
    Count: <span x-text="count"></span>
  </button>
  <div x-show="items.length > 0">
    <template x-for="item in items">
      <div x-text="item.name"></div>
    </template>
  </div>
</div>
```

### D3.js Integration Pattern
```html
<div x-data="{ 
  chartData: [], 
  width: 800, 
  height: 400 
}"
     x-init="$nextTick(() => initializeChart())"
     x-effect="updateChart()">
  <svg x-ref="chart" :width="width" :height="height"></svg>
</div>
```

### Event-Driven Communication
```html
<!-- Component A -->
<div x-data="{ selectedItem: null }">
  <button x-on:click="$dispatch('item-selected', { id: 123 })">
    Select Item
  </button>
</div>

<!-- Component B -->
<div x-data="{ activeItem: null }"
     x-on:item-selected.window="activeItem = $event.detail">
  <span x-text="activeItem?.id"></span>
</div>
```

<!-- MANUAL ADDITIONS START -->
<!-- MANUAL ADDITIONS END -->
